# Dataflow Processing Scheme

Because of the initial 'push' of genomes into the system, this scheme does not have the most optimal performance, but it is a clear way to model how a tower scheme should work.